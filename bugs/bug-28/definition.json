{
  "id": 28,
  "platform": "k8s",
  "affected-version": "<= v1.29.0",
  "affected-categorization": "api-machinery",
  "severity": "minor",
  "resolved": "closed",
  "link": "https://github.com/kubernetes/kubernetes/issues/119141",
  "description": "When performing a server-side apply PATCH request and encountering conflicts with differing field owners, Kubernetes responds with a 409 conflict error containing incorrect and missing conflicting fields in the error detail.",
  "expected-behaviour": "When encountering conflicts during a server-side apply PATCH request, Kubernetes should respond with a 409 conflict error containing all the correct conflicting fields in the error detail, without any duplicates or omissions."
}
