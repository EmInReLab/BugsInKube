{
  "id": 46,
  "platform": "k8s",
  "affected-version": "<= v1.28.0",
  "affected-categorization": "node",
  "severity": "minor",
  "resolved": "closed",
  "link": "https://github.com/kubernetes/kubernetes/issues/108152",
  "description": "A critical issue exists where Deployments are creating Pods in an infinite loop. This occurs when a Pod fails with an UnexpectedAdmissionError. While the Deployment's restartPolicy attempts to revive the Pod, the kubelet doesn't retry this specific error.",
  "expected-behaviour": "Kubelet will retry the UnexpectedAdmissionError and ReplicaSet controller will consider the failed pod to avoid control plane crushed."
}
